import pycountry
import pycountry_convert as Pc
from pycountry_convert import country_alpha2_to_continent_code , country_alpha2_to_country_name
import pytz
from datetime import datetime
#post continent.
'''country_code = Pc.country_name_to_country_alpha2("Nigeria", cn_name_format="default")
print(country_code)
continent_name = Pc.convert_country_alpha2_to_continent_code()
print(continent_name)
def country_to_continent(country_name):
    country_alpha2 =Pc.country_name_to_country_alpha2(country_name)
    country_continent_code = Pc.country_alpha2_to_continent_code(country_alpha2)
    country_continent_name = Pc.convert_continent_code_to_continent_name(country_continent_code)
    return country_continent_name

country_name = "France"
print(country_to_continent(country_name))'''

